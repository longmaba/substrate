# Rust Diff Noise Fixture

This fixture supports `substrate diff fixtures/diff-rust-pairs/before fixtures/diff-rust-pairs/after`.

It contains 10 Rust before/after pairs for the first structural diff spike and
one unsupported text pair to exercise fallback accounting. Pair classifications
and notes live in `NOTES.tsv`.
